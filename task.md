# Active Task: Initialize Project and Foundation

## Current Objectives
- [ ] Initialize Next.js application in the current directory.
- [ ] Clean up default boilerplate code.
- [ ] Setup CSS Variables and Global Styles for the design system.
- [ ] specific: "Use rich aesthetics, vibrant but calm colors, dark mode support in CSS variables."

## Next Steps
- [x] Create `package.json` and `tsconfig.json` manually.
- [x] Create `src/app` directory structure.
- [x] Implement `src/app/globals.css` with vibrant, premium design tokens (CSS variables).
- [x] Implement `src/app/layout.tsx` and `src/app/page.tsx` (Landing page).
- [x] Implement `AuthContext` and `RoleSwitcher` for role-based testing.
- [x] Create `src/app/dashboard/page.tsx` with role-specific views.
- [ ] Run `npm install` and `npm run dev` to verify the application.

